import { CardNotFound } from "@/components/cards/CardNotFound";

/** Rendered automatically by Next.js when notFound() is called in app/cards/[slug]/page.tsx. */
export default function NotFound() {
  return <CardNotFound />;
}